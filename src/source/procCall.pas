program Main;
   var x, y: real;
   var b : real;

   procedure Alpha(a : integer; b : integer);
      var y : integer;
   begin
      x := x + y + a + b;
   end;

begin { Main }
  Alpha(4 + 4, 7);
  { Alpha(4 + 4); }
  { Alpha(4); }
  { Alpha(); }
end.  { Main }
