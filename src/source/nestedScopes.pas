program Main;
   var x, y: real;

   procedure Alpha(a,b,c : integer; x:real);
      var y : integer;
   begin
      x := a + x + y;
   end;

   procedure Beta;
      var y : integer;
   begin
      x := x + y;
   end;

begin { Main }

end.  { Main }
